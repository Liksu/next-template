import { ReactElement } from 'react'
import { MenuList } from '@/interfaces/globals'

interface LeftPanelProps {
    menu?: MenuList
}

export default function LeftPanel({ menu }: LeftPanelProps): ReactElement {
    return (<>

    </>)
}
